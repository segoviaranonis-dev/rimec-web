/** Marca de caso en tarjeta — CHINELO · CHI · latido playa (≠ LIQ oro · ≠ PRO fucsia). */
export function PeChiBadge({ className = '' }: { className?: string }) {
  return (
    <span
      className={`catalog-pe-chi-badge inline-flex shrink-0 items-center rounded px-1 py-0.5 text-[7px] font-black uppercase leading-none tracking-wider shadow-sm sm:text-[8px] ${className}`}
      title="Caso CHINELO · marca Beira Rio · filtro Tipo CHINELO · COD.GRUPO 09 / BCL"
    >
      CHI
    </span>
  )
}
